server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Serve static files (don’t rewrite these)
  location /assets/ {
    try_files $uri =404;
  }

  # Fallback to index.html for SPA routes
  location / {
    try_files $uri $uri/ /index.html;
  }
}
